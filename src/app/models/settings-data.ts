export interface SettingsData {
  username: string;
  password: string;
  baseUri: string;
  accessToken: string;
  refreshToken: string;
  clientId: string;
  deviceId: string;
  semanticTopic: string;
  refreshInterval: number;
}
