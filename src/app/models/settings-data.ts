interface SettingsData {
  username: string;
  password: string;
  baseUri: string;
  accessToken: string;
  refreshToken: string;
  clientId: string;
  deviceId: string;
  assetName: string;
  refreshInterval: number;
}
